This directory stores uploaded medical images.
Subdirectories:
  originals/   — original uploads (never modified)
  processed/   — thumbnails and enhanced images

Both directories are created automatically on startup.
Add to .gitignore — never commit patient data.
